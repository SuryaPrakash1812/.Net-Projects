<Window x:Class="SqlExecuter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlExecuter"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Environment selection -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Environment: " FontWeight="Bold" VerticalAlignment="Center"/>
            <ComboBox x:Name="EnvCombo" Width="200" Margin="10,0"
                      SelectionChanged="EnvCombo_SelectionChanged"/>
            <Button Content="Execute SQL" Width="150" Click="ExecuteSql_Click"/>
        </StackPanel>

        <!-- SQL Editor -->
        <TextBox x:Name="SqlEditor"
                 Grid.Row="1"
                 FontFamily="Consolas"
                 FontSize="14"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>

        <!-- Results pane -->
        <DataGrid x:Name="ResultGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</Window>
